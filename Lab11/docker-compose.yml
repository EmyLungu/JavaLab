services:
  db:
    image: postgres:latest
    container_name: pg-lab11
    environment:
      POSTGRES_DB: lab11db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - pg-data:/var/lib/postgresql

volumes:
  pg-data:
